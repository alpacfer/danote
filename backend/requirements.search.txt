# Minimal backend dependencies for command search + wordbank add flow (no DaCy/NLP model).
# Keeps startup lightweight for investigating search behavior and UI interactions.

fastapi==0.116.1
uvicorn[standard]==0.35.0
PyJWT[crypto]==2.10.1
httpx==0.28.1
